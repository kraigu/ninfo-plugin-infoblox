% if (_ref == None):
No reference found.
% else:
Found ${dns_name}
${ipv4addr} ${dns_name}
Classification: ${classification}
Business Contact: ${businesscontact}
Technical Contact: ${technicalcontact}
% endif
